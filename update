#!/bin/bash
HAS_NVIM=false
VIM_COMMAND="vim"
if type "nvim" > /dev/null; then
	HAS_NVIM=true
	VIM_COMMAND="nvim"
fi


echo "Git pull"
git pull

echo "Update fzf"
cd ~/.fzf/
git pull
./install --all
cd -

echo "Update vim plugins"
nvim +PluginUpdate +qall

echo "GoUpdateBinaries"
nvim +GoUpdateBinaries +qall


echo "Install/update gocode"
go get -u github.com/nsf/gocode

echo "install padawan server"
nvim +"call deoplete#sources#padawan#UpdateServer()" +qa

if ! $HAS_NVIM; then
	echo "rebuild YouCompleteMe"
	cd ~/.vim/bundle/YouCompleteMe/
	./install.py --tern-completer --gocode-completer
	cd -
fi

echo "Done"
